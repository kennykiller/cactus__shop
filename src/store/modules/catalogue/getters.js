export default {
    catalogue(state) {
        return state.catalogue;
    },
    shortage(state) {
        return state.shortage;
    },
    filtered(state) {
        return state.filtered
    },
    id(state) {
        return state.id;
    }
}